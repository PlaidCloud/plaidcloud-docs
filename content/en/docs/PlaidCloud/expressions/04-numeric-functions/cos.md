---
title: COS
---

Returns the cosine of `x`, where `x` is given in radians.

## SQL Syntax

```sql
COS( <x> )
```

## SQL Examples

```sql
SELECT COS(PI());

┌───────────┐
│ cos(pi()) │
├───────────┤
│        -1 │
└───────────┘
```