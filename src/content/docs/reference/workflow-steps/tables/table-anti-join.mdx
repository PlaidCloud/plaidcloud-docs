---
title: Table Anti Join
description: Perform an anti-join between two tables in a PlaidCloud workflow step to find unmatched records that exist in only one table.
sidebar:
  order: 1
---

import SnippetCommonTableSourceSelection from '@snippets/common-table-source-selection.mdx';
import SnippetCommonTargetTableCreation from '@snippets/common-target-table-creation.mdx';
import SnippetCommonTableJoinMap from '@snippets/common-table-join-map.mdx';
import SnippetCommonDataMapper from '@snippets/common-data-mapper.mdx';
import SnippetCommonDataFilter from '@snippets/common-data-filter.mdx';
import { Aside } from '@astrojs/starlight/components';

## Description


Table Anti Join provides the unmatched set of items between two tables. This will return the list of items in the first table without matches in the second table. This can be quite useful for determining which records are present in one table but not another.



This operation could be accomplished by using outer joins and filtering on null values for the join; however, the Anti Join transform will perform this in a more efficient and obvious way.

<Aside type="note" title="This is a left-excluding join, not a left join">
Table Anti Join keeps the rows of source A that have **no** match in source B. Its behavior is fixed by the step type, so there is no join-type selector. If you want a **left join** — every row of source A, with source B's columns filled in where the keys match — use the [Table Outer Join](/reference/workflow-steps/tables/table-outer-join/) step and set its **Join Type** to **Left**. Because an anti-join returns only unmatched source A rows, its output is drawn from **source A only**; source B contributes the join keys but no output columns.
</Aside>



## Table Data Selection




<SnippetCommonTableSourceSelection />


## Table Output




<SnippetCommonTargetTableCreation />


### Join Map

<SnippetCommonTableJoinMap />

### Target Output Columns


<SnippetCommonDataMapper />


## Output Filters


<SnippetCommonDataFilter />
