---
title: Table Append
description: Append rows to an existing data table in a PlaidCloud workflow step to add new records while preserving existing table data.
sidebar:
  order: 2
---

import SnippetCommonDataMapper from '@snippets/common-data-mapper.mdx';
import SnippetCommonDataFilter from '@snippets/common-data-filter.mdx';

## Description

Adds rows from a source table to the end of an existing target table. Existing rows in the target are preserved; only new rows are inserted. Use this when you have data arriving incrementally (e.g., daily extracts, monthly load files) and want to grow the target over time rather than replacing it.

If you need to deduplicate as you append, follow with a [Table Union Distinct](/reference/workflow-steps/tables/table-union-distinct/) step, or use [Table Upsert](/reference/workflow-steps/tables/table-upsert/) if some rows update existing records by key.

## Load Parameters

### Source and Target

![Source And Target](/images/table_source_and_target_EN.png)

To establish the source and target tables, first select the data table to be extracted from using the **Source Table** dropdown menu. Next, select an existing table as the target table using the **Target Table** dropdown.


## Table Data Selection

When configuring the Data Mapper the columns in the source table must be mapped to a column in the target table.

<SnippetCommonDataMapper />


## Data Filters

<SnippetCommonDataFilter />



## **examples**
