---
title: "Import Fusion Entity"
description: Pull an Oracle Fusion Cloud GL balances or journal batches entity into a project table over OData/BIP, with server-side date, period, and numeric range filters.
---

import { Aside } from '@astrojs/starlight/components';

## Description

Imports one Oracle Fusion Cloud entity over your [Oracle Fusion connection](/guides/connections/oracle-fusion/) into a project table, calling Fusion's OData or BI Publisher (BIP) endpoints directly. Pick the connection, the entity type, and any filters; the step pulls matching records and writes them, with no OData query to author.

For a pull too large for a direct call, use [Import Fusion Bulk Extract](/reference/workflow-steps/oracle-fusion/import-fusion-bulk-extract/) instead.

## Configuration

| Field | Required | Notes |
|---|---|---|
| Fusion Connection | Yes | The Oracle Fusion connection to read through. |
| Target Table | Yes | The project table the results are written to. It is overwritten in full on every run. |
| Entity Type | Yes | Which entity to pull. Default `gl_balances`. One of `gl_balances`, `journal_batches`. |
| Filters | No | Range filters (`ge`/`le`) on date, accounting-period, and numeric balance/amount fields, plus equality filters on discrete fields (ledger, currency). Each entity restricts filters to the fields Oracle Fusion can actually evaluate server side — an unsupported field is rejected, not silently dropped. |

### Entities and Filterable Fields

| Entity Type | What it pulls | Filterable fields |
|---|---|---|
| `gl_balances` | General ledger account balances | accounting period range (`ge`/`le`), account range, balance amount range (`ge`/`le`) |
| `journal_batches` | Journal batch headers and status | date range (`ge`/`le`), accounting period range (`ge`/`le`) |

<Aside type="note">
A range filter needs only one bound to be useful — `accounting_date ge 2026-01-01` with no upper bound pulls everything from that date forward. Supply both bounds to scope a pull tightly.
</Aside>

## Related

- [Oracle Fusion Steps](/reference/workflow-steps/oracle-fusion/)
- [Import Fusion Bulk Extract](/reference/workflow-steps/oracle-fusion/import-fusion-bulk-extract/) — for pulls too large for this step
- [Connect to Oracle Fusion](/guides/connections/oracle-fusion/) — step-by-step walkthrough
- [Oracle Fusion Connector](/reference/connectors/erp/oracle-fusion/)
