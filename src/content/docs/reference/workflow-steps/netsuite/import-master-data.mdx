---
title: "NetSuite: Import Master Data"
description: Pull NetSuite master lists — accounts, subsidiaries, departments, classes, locations, vendors, or customers — into a project table, with no SuiteQL to write.
---

import { Aside } from '@astrojs/starlight/components';

## Description

Imports a NetSuite master-data list over your [NetSuite connection](/guides/connections/netsuite/) into a project table. Pick one record type — accounts, subsidiaries, departments, classes, locations, vendors, or customers — and the step pulls the list and writes it, with no SuiteQL to author or maintain.

Master data is the reference data the balance and detail pulls point at: the accounts, subsidiaries, and dimensions your GL rows carry as internal IDs. Import the list you need to resolve those internal IDs to names, or to drive lookups and joins downstream. This is one of the canned [NetSuite import steps](/reference/workflow-steps/netsuite/), which run on a shared read layer that refreshes the OAuth token, pages through large results, and retries transient errors for you.

## Configuration

| Field | Required | Notes |
|---|---|---|
| NetSuite Connection | Yes | The NetSuite connection to read through, filtered to connections of type NetSuite. |
| Target Table | Yes | The project table the list is written to. It is overwritten in full on every run. |
| Record Type | Yes | Which master list to pull: **account** (the default), **subsidiary**, **department**, **class**, **location**, **vendor**, or **customer**. |
| Include Inactive | No | Off by default, returning only active records. Turn it on to include records marked inactive in NetSuite. |

<Aside type="note">
Use this step to build the small reference tables the other NetSuite steps rely on — for example, pull **subsidiary** once to map a subsidiary internal ID to its name before restricting an [Import Balances](/reference/workflow-steps/netsuite/import-balances/) or [Import GL Detail](/reference/workflow-steps/netsuite/import-gl-detail/) pull to one subsidiary.
</Aside>

## Related

- [NetSuite Steps](/reference/workflow-steps/netsuite/)
- [Import NetSuite Financials](/guides/connections/import-netsuite-financials/) — step-by-step walkthrough
- [Connect to NetSuite](/guides/connections/netsuite/)
- [NetSuite REST Connector](/reference/connectors/rest/netsuite/)
