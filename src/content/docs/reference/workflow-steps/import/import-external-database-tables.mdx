---
title: Import External Database Tables
description: Import data from external database tables into PlaidCloud as a workflow step to bring data from connected database sources.
sidebar:
  order: 4
---

import SnippetCommonRemoveNonAscii from '@snippets/common-remove-non-ascii.mdx';
import SnippetCommonDeleteFilesAfterImport from '@snippets/common-delete-files-after-import.mdx';
import SnippetCommonImportFileSelection from '@snippets/common-import-file-selection.mdx';
import SnippetCommonImportTargetSelection from '@snippets/common-import-target-selection.mdx';
import SnippetCommonDataMapper from '@snippets/common-data-mapper.mdx';
import SnippetCommonDataFilter from '@snippets/common-data-filter.mdx';
import { Aside } from '@astrojs/starlight/components';

## Description


Imports one or more tables from an external database (configured via a database connector) into project tables. Supports **delta loads** — only pulling rows that have changed since the last import — and **table name remapping** — landing the source table under a different name in PlaidCloud.

Use this for keeping PlaidCloud tables in sync with operational source systems on a schedule, without re-importing every row each time.


## Examples

No examples yet...

---

## Unique Configuration Items

None

---

## Common Configuration Items

<SnippetCommonRemoveNonAscii />

<SnippetCommonDeleteFilesAfterImport />

<SnippetCommonImportFileSelection />

<SnippetCommonImportTargetSelection />

<SnippetCommonDataMapper />

<Aside title="Inspect Source reads the external database">
For a table added to this step, the **Inspect Source** button reads that table's current columns straight from the connected database and refreshes just that table's mapping in place. Use it when a source table's columns have changed since you set the step up, without re-running discovery over the whole connection.
</Aside>

<SnippetCommonDataFilter />
