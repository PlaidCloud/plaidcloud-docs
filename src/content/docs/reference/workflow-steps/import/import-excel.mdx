---
title: Import Excel
description: Import Excel spreadsheet files into PlaidCloud as a workflow step with options for sheet selection, ranges, and data type mapping.
sidebar:
  order: 3
---

import { Aside } from '@astrojs/starlight/components';
import SnippetNoExamples from '@snippets/no-examples.mdx';
import SnippetCommonImportFileSourceTargetSelection from '@snippets/common-import-file-source-target-selection.mdx';
import SnippetCommonRemoveNonAscii from '@snippets/common-remove-non-ascii.mdx';
import SnippetCommonDeleteFilesAfterImport from '@snippets/common-delete-files-after-import.mdx';
import SnippetCommonSkipIfUnchanged from '@snippets/common-skip-if-unchanged.mdx';
import SnippetCommonDataMapper from '@snippets/common-data-mapper.mdx';
import SnippetCommonDataFilter from '@snippets/common-data-filter.mdx';

## Description

Import specific worksheets from Microsoft Excel files from PlaidCloud Document. Analyze supports the legacy Excel format (XP/2003) as well as the new format (2007/2010/2013). This includes, but is not limited to, the following file types:

* XLS
* XLSX
* XLSB
* XLSM

Excel imports stream through temporary storage rather than loading the whole workbook into memory, so very large files import without running out of memory. Source data types — decimals, dates, and timestamps — are preserved in the target table instead of passing through text.

## Examples

<SnippetNoExamples />

---

## Import Parameters

<SnippetCommonImportFileSourceTargetSelection />

<SnippetCommonRemoveNonAscii />

<SnippetCommonDeleteFilesAfterImport />

<SnippetCommonSkipIfUnchanged />

### Header

Since Excel files may or may not contain headers, PlaidCloud provides a way to either use the headers, ignore headers, or use column order to determine the column alignment.
* **No Header**: The file contains no header.  Use the source list in the *Data Mapper* to determine the column alignment
* **Has Header - Use Header and Override Field List**: The file has a header.  Use the header names specified and ignore the source list in the *Data Mapper*.
* **Has Header - Skip Header and Use Field List Instead**: The file has a header but it should be ignored.  Use the header names specified by the source list in the *Data Mapper*.

### Row Selection

For input files with extraneous records, you can specify a number of rows to skip before processing the data.  This is useful if files contain header blocks that must be skipped before arriving at the tabular data.

### What to Read

Each entry in the import selects a worksheet, a workbook-defined named range, or a specific cell range — the three are peers, and an import can mix them across multiple entries.

* **Worksheet**: Reads a full sheet, selected by name.
* **Named Range**: Reads a range the workbook itself defines a name for. Named ranges are listed alongside worksheets in the picker (see *Find Sheets in Selected File* below), so you select one the same way you'd select a sheet.
* **Cell Range**: Reads a specific rectangular range that isn't backed by a worksheet or workbook name. Pick the sheet the range lives on, then enter the range in `A1:J100` style — for example, `Sheet1$A1:J100`.

<Aside type="caution">A named range that refers to more than one disjoint area, or a cell range that is out of the sheet's bounds or inverted (e.g. the end cell above or to the left of the start cell), is rejected with an error rather than imported — so an import never silently reads the wrong cells.</Aside>

#### Worksheets to Import

Because workbooks may contain many worksheets with different data, it is possible to select which worksheets should be imported in the current import process.  The options are:

* All Worksheets
* Worksheets Matching Search
* Selected Worksheets

#### Using Worksheet Search

The search functionality for worksheets allows inclusion of worksheets matching the search criteria.  The search criteria allows for:
* **Starts With**: The worksheet name starts with the search text
* **Contains**: The worksheet name contains the search text
* **Ends With**: The worksheet name ends with the search text

#### Find Sheets in Selected File

The find sheets button will open the Excel file and list the worksheets and named ranges available in the table.  Mark the checkboxes in the table for the worksheets and/or named ranges to be included in the import.

<Aside>When populating the *Data Mapper*, the first worksheet, named range, or cell range found in the list will be used. Ensure everything included in the import step has a similar format.</Aside>

<Aside>An empty worksheet in a multi-worksheet import loads zero rows from that worksheet — it does not fail the import.</Aside>

## Table Data Selection

<SnippetCommonDataMapper />

## Data Filters

<SnippetCommonDataFilter />
