---
title: Import Sage
description: Import Sage Intacct account balances by dimension for a date and account range — a trial balance, with the dimension columns you select carried onto every row.
sidebar:
  order: 27
---

import SnippetCommonImportTargetSelection from '@snippets/common-import-target-selection.mdx';
import SnippetCommonDataMapper from '@snippets/common-data-mapper.mdx';
import SnippetCommonDataFilter from '@snippets/common-data-filter.mdx';

## Description

Imports Sage Intacct **account balances by dimension** over a Sage connection (Intacct's `get_accountbalancesbydimensions`), bounded by a date range and an account range, with the dimensions you select carried onto each row. The result is a trial balance: one row per account per dimension combination, carrying an opening (`startbalance`), period (`periodbalance`), and ending (`endbalance`) balance.

Use this for a trial balance or a balances-based P&L. For transaction-level general ledger detail, use [Import Sage Intacct Query](/reference/workflow-steps/import/import-intacct-query/) against `GLDETAIL`; for an ad-hoc query against any Intacct object, use the same step with your object name.

## Unique Configuration Items

### Source and Target

- **Connection** — the Sage connection to read through.
- **Company** — the Sage company to read from.

### Data Selection

- **Start date** / **End date** — the period the balances are reported over (both are required; a lone start or end date is ignored).
- **Start account** / **End account** — the account-code range to include.
- **Entities** — the Sage entities to include.
- **Dimensions** — switch on the dimensions you want as columns: class, contact, cost type, customer, department, employee, item, location, project group, task, and vendor.
- **Custom dimensions** — any additional user-defined dimensions to carry through.

:::caution[The Warehouse checkbox does nothing]
The step configuration offers a **Warehouse** dimension, but the workflow runner
does not read it — the dimension is absent from the set it builds, so switching
it on adds no column. Use **Custom dimensions** if you need warehouse on the
output.
:::

## Common Configuration Items

<SnippetCommonImportTargetSelection />

<SnippetCommonDataMapper />

<SnippetCommonDataFilter />

## Related

- [Import steps](/reference/workflow-steps/import/)
- [Import Sage Intacct Query](/reference/workflow-steps/import/import-intacct-query/)
- [Import Sage AP](/reference/workflow-steps/import/import-sage-ap/)
- [Export Sage Trial Balance](/reference/workflow-steps/export/export-to-sage/)
