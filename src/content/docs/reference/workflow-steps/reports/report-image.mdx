---
title: Report Image
description: Place an image into a composed PlaidCloud report.
sidebar:
  order: 15
---

## Description

The **Image** placed into a composed report. It is the equivalent of the matching Alteryx *Reporting* / *Portfolio Composer* tool and runs in the workflow engine (reportlab / matplotlib); the renderer is an approximation of the Alteryx output.

## Source & Options

- **Source Table** — the data feeding this report piece.
- **Output Table** — the table the rendered snippet is written to.
- **Report order** — the order of this piece within a composed report.
- **Image mode** — Static (an inline base64 image) or Field (a per-row file path).
- **Base64 image** — the inline image data for Static mode.

## Columns

The **Columns** tab maps the source columns available to the report piece.
