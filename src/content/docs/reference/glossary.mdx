---
title: Glossary
description: Definitions for the terms used throughout PlaidCloud documentation.
---

export const glossarySchema = {
	'@context': 'https://schema.org',
	'@type': 'DefinedTermSet',
	name: 'PlaidCloud Glossary',
	description: 'Canonical definitions for product terms used throughout PlaidCloud documentation.',
	hasDefinedTerm: [
		{ '@type': 'DefinedTerm', name: 'Account',         description: 'A PlaidCloud login that belongs to one or more organizations and is granted access to specific workspaces.' },
		{ '@type': 'DefinedTerm', name: 'AI Assistant',    description: 'A project-scoped chat interface inside PlaidCloud for asking questions about data, generating expressions, and operating on projects in natural language.' },
		{ '@type': 'DefinedTerm', name: 'Allocation',      description: 'A workflow operation that spreads values from one set of rows to another based on driver data and rules — used for transfer pricing, activity-based costing, IT chargeback, and profitability analysis.' },
		{ '@type': 'DefinedTerm', name: 'Connection',      description: 'A saved configuration that lets PlaidCloud reach an external system (database, cloud storage, ERP, REST API). Reused by workflow steps so credentials live in one place.' },
		{ '@type': 'DefinedTerm', name: 'Dash App',        description: 'A custom interactive application built on PlaidCloud data using the Plotly Dash framework, hosted at parity with a server Panel app.' },
		{ '@type': 'DefinedTerm', name: 'Dashboard',       description: 'A published, interactive view of project data built from tables and views.' },
		{ '@type': 'DefinedTerm', name: 'Dimension',       description: 'A hierarchy used for slicing and aggregating data — cost centers, products, geography, time periods. Allocations use dimensions to decide what to allocate to what.' },
		{ '@type': 'DefinedTerm', name: 'Driver',          description: 'In an allocation, the data that determines how values are spread across targets. Example: a headcount table driving IT cost allocation by business unit.' },
		{ '@type': 'DefinedTerm', name: 'Lakehouse',       description: "PlaidCloud's underlying analytic data store. Versioned: Lakehouse v1 (Databend-based) and Lakehouse v2 (StarRocks-based)." },
		{ '@type': 'DefinedTerm', name: 'MCP',             description: 'Model Context Protocol — the standard PlaidCloud uses to integrate with AI coding agents like Claude Code, Cursor, and Copilot.' },
		{ '@type': 'DefinedTerm', name: 'Member',          description: 'A user account granted access to a workspace. Members are assigned to security groups that control what they can do.' },
		{ '@type': 'DefinedTerm', name: 'Organization',    description: 'The top-level billing and identity boundary in PlaidCloud. An organization contains one or more workspaces and member accounts.' },
		{ '@type': 'DefinedTerm', name: 'Panel App',       description: 'A custom interactive application built on PlaidCloud data, typically using the Panel framework.' },
		{ '@type': 'DefinedTerm', name: 'PlaidLink',       description: "PlaidCloud's on-premises connector daemon — runs as a Windows service, Unix daemon, or container to bridge PlaidCloud workflows to local data sources." },
		{ '@type': 'DefinedTerm', name: 'PlaidXL',         description: 'The PlaidCloud Excel add-in for retrieving and refreshing data from project tables inside Excel.' },
		{ '@type': 'DefinedTerm', name: 'Project',         description: 'A unit of work inside a workspace. Each project owns its own tables, workflows, dimensions, and audit history. Projects are isolated from each other.' },
		{ '@type': 'DefinedTerm', name: 'Publish',         description: 'Make a project table available for external consumption — dashboards, BI tools, downstream systems.' },
		{ '@type': 'DefinedTerm', name: 'Security Group',  description: 'A bundle of permissions inside a workspace. Members are assigned to security groups to grant specific capabilities.' },
		{ '@type': 'DefinedTerm', name: 'Step',            description: 'A single operation inside a workflow — import a CSV, join two tables, send a notification, run an allocation.' },
		{ '@type': 'DefinedTerm', name: 'Table',           description: 'Structured row-and-column data inside a project. Tables come from imports, transformations, or external sources.' },
		{ '@type': 'DefinedTerm', name: 'View',            description: 'A saved query result layered on top of one or more tables. Behaves like a table for downstream consumers, but the underlying data is re-evaluated on read.' },
		{ '@type': 'DefinedTerm', name: 'Workflow',        description: 'A sequence of steps that operates on tables. The unit of automation in PlaidCloud — workflows can run sequentially, in parallel, conditionally, or in loops.' },
		{ '@type': 'DefinedTerm', name: 'Workspace',       description: 'An isolated environment inside an organization where actual work happens. Teams typically use one workspace per environment (dev, staging, prod) or per business unit.' },
	],
};

<script type="application/ld+json" set:html={JSON.stringify(glossarySchema)} />

A single canonical place for product terms. If something in a guide or reference page uses a word here, this is what it means.

## Account

A PlaidCloud login. An account belongs to one or more organizations and is granted access to specific workspaces within those organizations.

## AI Assistant

A project-scoped chat interface inside PlaidCloud for asking questions about data, generating expressions, and operating on projects in natural language. See [AI Assistant guide](/guides/ai-assistant/).

## Allocation

A workflow operation that spreads values from one set of rows ("source") to another ("target") based on driver data and rules. Used for transfer pricing, activity-based costing, IT chargeback, profitability analysis, and similar cost-distribution problems. See [Allocations](/guides/allocations/).

## Connection

A saved configuration that lets PlaidCloud reach an external system — a database, cloud storage account, ERP, REST API. Connections are reused by workflow steps so credentials live in one place. See [Connections (guide)](/guides/connections/) · [Connectors (reference)](/reference/connectors/).

## Dash App

A custom interactive application built on PlaidCloud data using the Plotly Dash framework, published from a Git repository and hosted at parity with a server Panel app. See [Dash apps](/guides/dash-apps/).

## Dashboard

A published, interactive view of project data built from tables and views. See [Dashboards](/guides/dashboards/).

## Dimension

A hierarchy — used for slicing and aggregating data. Cost centers, products, geography, and time periods are common dimensions. Allocations use dimensions to decide what to allocate to what. See [Dimensions](/guides/dimensions/).

## Driver

In an allocation, the data that determines *how* values are spread across targets. Example: if you're allocating IT cost to business units by headcount, the headcount table is the driver.

## Lakehouse

PlaidCloud's underlying analytic data store. The expression library is versioned by Lakehouse engine: **Lakehouse v1** (Databend-based) and **Lakehouse v2** (StarRocks-based). See [Expressions reference](/reference/expressions/).

## MCP

Model Context Protocol — the standard PlaidCloud uses to integrate with AI coding agents (Claude Code, Cursor, Copilot, etc.). See [AI coding agents](/integrations/ai-coding-agents/).

## Member

A user account granted access to a workspace. Members are assigned to security groups within the workspace, which control what they can do. See [Access management](/administration/access/).

## Organization

The top-level billing and identity boundary in PlaidCloud. An organization contains one or more workspaces and one or more member accounts.

## Panel App

A custom interactive application built on PlaidCloud data, typically using the Panel framework. See [Panel apps](/guides/panel-apps/).

## PlaidLink

PlaidCloud's on-premises connector daemon — runs as a Windows service, Unix/Linux/Mac daemon, or container service to bridge PlaidCloud workflows to local data sources. See [PlaidLink reference](/reference/cli/plaidlink/).

## PlaidXL

The PlaidCloud Excel add-in for retrieving and refreshing data from project tables inside Excel. See [PlaidXL reference](/reference/cli/plaidxl/).

## Project

A unit of work inside a workspace. Each project owns its own tables, workflows, dimensions, and audit history. Projects are isolated from each other — they don't share state. See [Projects](/guides/projects/).

## Publish

Make a project table available for external consumption (dashboards, BI tools, downstream systems). See [Publishing data](/guides/data/publish/).

## Security Group

A bundle of permissions inside a workspace. Members are assigned to security groups to grant them specific capabilities (read tables, edit workflows, manage members, etc.). See [Security groups](/administration/access/managing-security-groups-and-assignments/).

## Step

A single operation inside a workflow — import a CSV, join two tables, send a notification, run an allocation. Steps come in categories (import, export, tables, allocation, dimension, document, notifications, etc.). See [Workflow steps reference](/reference/workflow-steps/).

## Table

Structured row-and-column data inside a project. Tables come from imports, transformations, or external sources.

## View

A saved query result layered on top of one or more tables. Behaves like a table for downstream consumers but the underlying data is re-evaluated on read.

## Workflow

A sequence of steps that operates on tables. The unit of automation in PlaidCloud. Workflows can run sequentially, in parallel, conditionally, or in loops. See [Workflows (guide)](/guides/workflows/) · [Workflow steps (reference)](/reference/workflow-steps/).

## Workspace

An isolated environment inside an organization where actual work happens. Teams typically use one workspace per environment (dev, staging, prod) or per business unit. Members are granted access at the workspace level. See [Access management](/administration/access/).
