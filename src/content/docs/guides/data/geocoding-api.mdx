---
title: Migrating From Google or Mapbox Geocoding
description: Move a Google Geocoding API or Mapbox Geocoding integration to PlaidCloud's REST geocoding endpoints — endpoint mapping, the response envelope, location_type equivalents, authentication, rate limits, and honest coverage limits.
sidebar:
  label: Geocoding API
  order: 10
---

import { Aside } from '@astrojs/starlight/components';

PlaidCloud exposes forward geocoding, structured geocoding, reverse geocoding, autocomplete, and place lookup as REST endpoints under `/rest/v1/analyze/geocode`. They proxy an in-cluster [Pelias](https://pelias.io) service backed by OpenStreetMap data, and wrap every result in a small `plaidcloud` envelope so a caller can tell how precise a match is without parsing Pelias' internal fields directly.

This guide is for anyone moving an existing Google Geocoding API or Mapbox Geocoding integration onto PlaidCloud — request shapes that map closely, and the places they don't.

<Aside type="caution" title="Read this before you migrate">
PlaidCloud's geocoder is OpenStreetMap-derived, not a licensed commercial dataset. It is not a drop-in replacement for every Google or Mapbox use case — see [Coverage and Determinism](#coverage-and-determinism-read-this-first) before you commit to it for anything address-critical.
</Aside>

## Authentication and Entitlement

Every geocoding call needs:

- A normal PlaidCloud session or bearer token carrying the **`analyze.table.read`** role — the same role that gates other read access to a project's data. There is no geocoding-specific role.
- The **`flag.geocode`** entitlement enabled on your tenant. A tenant without it gets a `403` on every call.

A tenant can also have the geocode router disabled outright as an operational kill switch, independent of the entitlement — that returns `503 geocoding_unavailable` (see [Errors](#errors) below). If you get a 503 immediately with no apparent Pelias issue, ask your PlaidCloud administrator whether the router is disabled for your tenant.

## Rate Limits

The geocoding endpoints carry a raised cap over PlaidCloud's default REST rate limit: **1,200 requests per minute per calling IP**, versus 600/minute on most other `/rest/v1` endpoints. Exceeding it returns `429` with a `Retry-After: 60` header.

There is no per-request caching in front of Pelias — every call is answered live. Cache results on your own side if you expect to repeat a query.

## Endpoint Mapping

| Your call today | PlaidCloud endpoint | Notes |
|---|---|---|
| Google Geocoding API (address → coordinates) | `GET /rest/v1/analyze/geocode/search` | Free-text query |
| Google Geocoding API (structured components) | `GET /rest/v1/analyze/geocode/search/structured` | Field-by-field |
| Google Reverse Geocoding | `GET /rest/v1/analyze/geocode/reverse` | Coordinates to places |
| Google Places Autocomplete | `GET /rest/v1/analyze/geocode/autocomplete` | Type-ahead. Degrades to empty results instead of returning an error — see [Autocomplete degrades, it doesn't error](#autocomplete-degrades-it-doesnt-error) |
| Google Place Details (`place_id` lookup) | `GET /rest/v1/analyze/geocode/place/{gid}` | `gid` is **not** a permanent identifier the way `place_id` is — see [Coverage and Determinism](#coverage-and-determinism-read-this-first) |
| Mapbox Geocoding API (forward) | `GET /rest/v1/analyze/geocode/search` or `/search/structured` | |
| Mapbox Geocoding API (reverse) | `GET /rest/v1/analyze/geocode/reverse` | |

### `search` — Free-Text Forward Geocoding

| Query parameter | Type | Notes |
|---|---|---|
| `text` | string, required | 1–300 characters |
| `focus.point.lat`, `focus.point.lon` | float | Soft ranking bias, not a filter |
| `boundary.country` | string | Hard filter — comma-separated ISO alpha-2 country codes, max 300 chars |
| `layers` | string | Comma-separated Pelias layers to restrict to, max 300 chars |
| `size` | integer | 1–40, default 10 |

```
GET /rest/v1/analyze/geocode/search?text=350+5th+Ave%2C+New+York&size=5
```

### `search/structured` — Field-by-Field Forward Geocoding

| Query parameter | Type | Notes |
|---|---|---|
| `address` | string | House number + street |
| `neighbourhood`, `borough`, `county` | string | |
| `locality` | string | City |
| `region` | string | State/province |
| `postalcode` | string | |
| `country` | string | ISO alpha-2/3 or full country name |
| `size` | integer | 1–40, default 10 |

Every field above is optional and capped at 300 characters. Supply as many as you have — it does not require all of them.

```
GET /rest/v1/analyze/geocode/search/structured?address=350+5th+Ave&locality=New+York&region=NY&country=US
```

### `reverse` — Coordinates to Nearest Places

| Query parameter | Type | Notes |
|---|---|---|
| `point.lat` | float, required | -90 to 90 |
| `point.lon` | float, required | -180 to 180 |
| `size` | integer | 1–40, default 1 |
| `layers` | string | Comma-separated Pelias layers, max 300 chars |

```
GET /rest/v1/analyze/geocode/reverse?point.lat=40.7484&point.lon=-73.9857
```

### `autocomplete` — Type-Ahead

| Query parameter | Type | Notes |
|---|---|---|
| `text` | string, required | 1–300 characters, partial query |
| `focus.point.lat`, `focus.point.lon` | float | Soft ranking bias |
| `layers` | string | Comma-separated Pelias layers, max 300 chars |

```
GET /rest/v1/analyze/geocode/autocomplete?text=350+5th
```

### `place/{gid}` — Resolve a Place Record

| Path parameter | Type | Notes |
|---|---|---|
| `gid` | string, required | 1–300 characters — a Pelias global id, taken from a prior result's `properties.gid` |

```
GET /rest/v1/analyze/geocode/place/openstreetmap:venue:node/61785451
```

## The `plaidcloud` Response Envelope

Every endpoint returns Pelias' own GeoJSON `FeatureCollection` unmodified, with one addition: a `plaidcloud` object attached to **each feature**.

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "geometry": { "type": "Point", "coordinates": [-73.9857, 40.7484] },
      "properties": {
        "gid": "openstreetmap:venue:node/61785451",
        "layer": "venue",
        "name": "Empire State Building",
        "confidence": 0.94,
        "accuracy": "point",
        "label": "Empire State Building, New York, NY, USA"
      },
      "plaidcloud": {
        "attribution": "Basemap and geocoding data © OpenStreetMap contributors, licensed under ODbL (https://www.openstreetmap.org/copyright).",
        "location_type": "geometric_center",
        "confidence": 0.94,
        "match_type": "exact"
      }
    }
  ]
}
```

Fields inside `plaidcloud`:

| Field | Type | Notes |
|---|---|---|
| `attribution` | string | Fixed OSM ODbL attribution string — see [Attribution](#attribution-is-not-optional) below |
| `location_type` | string | See [`location_type` mapping](#location_type-mapping-to-googles-taxonomy) |
| `confidence` | float or `null` | Pelias' own raw confidence score, passed through unmodified — PlaidCloud does not recalibrate or document its scale beyond what Pelias publishes |
| `match_type` | `"exact"` \| `"partial"` | **`search` only.** `"exact"` when `confidence >= 0.9`, else `"partial"`. Not present on `search/structured`, `reverse`, `autocomplete`, or `place` responses |

**A query with zero matches is a `200`, not an error.** When Pelias returns no features, the response carries an **empty `features` array** and a **top-level** `plaidcloud` object instead of per-feature ones:

```json
{
  "type": "FeatureCollection",
  "features": [],
  "plaidcloud": {
    "attribution": "Basemap and geocoding data © OpenStreetMap contributors, licensed under ODbL (https://www.openstreetmap.org/copyright).",
    "location_type": "none"
  }
}
```

### `location_type` Mapping to Google's Taxonomy

Pelias' `layer` and `accuracy` fields are collapsed into a four-value `location_type`, chosen to line up with Google Geocoding's own `location_type` enum:

| PlaidCloud `location_type` | Meaning | Nearest Google equivalent |
|---|---|---|
| `rooftop` | Address-level match with a precise point | `ROOFTOP` |
| `interpolated` | Address matched by interpolating along a street segment, not a directly geocoded point | `RANGE_INTERPOLATED` |
| `geometric_center` | A venue or POI's center point (a building, a business) | `GEOMETRIC_CENTER` |
| `approximate` | Anything coarser — locality, region, country level, or a layer PlaidCloud doesn't otherwise classify | `APPROXIMATE` |
| `none` | Zero matching features | *(no Google equivalent — Google returns no results at all rather than a typed empty result)* |

This is deliberately a coarse, four-value mapping rather than a full replica of Pelias' own layer taxonomy — enough to tell a caller how much to trust a match's precision, not a precision guarantee.

### Attribution Is Not Optional

The geocoding and basemap data behind this API is OpenStreetMap data, licensed under the **Open Database License (ODbL)**. ODbL requires that any use of the data credit OpenStreetMap. **If you display geocoding results to an end user — on a map, in an address list, anywhere — you must display the `attribution` string** (or equivalent OSM credit) alongside them. This is a licensing obligation on you as the consumer of the data, not a PlaidCloud house-style suggestion.

## Errors

| Status | Meaning |
|---|---|
| `403` | Missing the `analyze.table.read` role, or the tenant's `flag.geocode` entitlement isn't enabled |
| `404` | `place/{gid}` found no record for that id, or `reverse` hit a coordinate Pelias itself 404s on |
| `422` | A query parameter failed validation — for example `text` over 300 characters, or `point.lat` out of range |
| `429` | Rate limit exceeded — see [Rate Limits](#rate-limits) |
| `503 geocoding_unavailable` | Pelias could not be reached, timed out, or returned an error — **or** the tenant's geocode router is administratively disabled |

A `503` here is deliberately never a raw `5xx` and never an unbounded hang: the proxy applies a bounded timeout (a few seconds) to every call to Pelias, so a slow or hung upstream surfaces quickly as `503` rather than leaving your request open.

### Autocomplete Degrades, It Doesn't Error

`autocomplete` is the one exception to the `503` behavior above. Because it fires on every keystroke, a transient Pelias hiccup there returns a **`200` with an empty result set** (the same shape as a genuine zero-match query — `location_type: "none"`) rather than a `503`. If you're migrating from Google Places Autocomplete, where a backend failure surfaces as an error your client code branches on, note that PlaidCloud's autocomplete cannot be distinguished from "no suggestions" by status code alone — build your UI around an empty suggestion list either way, rather than relying on an error path here.

## Coverage and Determinism — Read This First

Be honest with yourself about what this API is before pointing production traffic at it:

- **It's OpenStreetMap-derived, not a licensed commercial address dataset.** Coverage quality tracks how well an area is mapped in OSM — generally strong in North America, Europe, and other well-mapped urban regions, and inconsistent elsewhere. There is no PlaidCloud-published coverage guarantee by country or region.
- **Addresses are frequently not interpolated to a true rooftop point.** Where OSM (or the open address data behind it) doesn't carry a directly geocoded point for an address, you'll get an `interpolated` or `approximate` result rather than `rooftop` — this is common, not an edge case, and varies by region.
- **No apartment/suite/unit-level detail.** Subpremise information isn't returned; you'll get building- or street-level precision at best.
- **No deep administrative hierarchy.** If you're used to Google's multi-level `address_components` (sublocality tiers, `administrative_area_level_1/2/3`, and so on), expect a shallower breakdown here.
- **`gid` is not a permanent identifier.** Google's `place_id` is a stable, long-lived reference you can store and re-resolve months or years later. Pelias' `gid` carries no such guarantee — treat it as valid for the duration of a session or a short-lived workflow, not as a durable foreign key into your own data.
- **Results are not guaranteed deterministic over time.** OpenStreetMap is continuously, crowd-sourced edited — the same query run today and again in three months can return different results, a different `gid`, or a different ranking, because the underlying map data itself changed. If your migration plan assumes byte-for-byte reproducible geocoding results the way a versioned commercial dataset would give you, this API does not provide that.
- **No response caching in v1.** Every call is answered live against the current OSM-derived index; there's no ETag or cache-control story to lean on yet.

None of this makes the API unsuitable — for address normalization, map display, or general-purpose location lookup it's a solid fit. It's specifically the wrong tool if you need permanent place identifiers, guaranteed rooftop precision everywhere, or bit-for-bit reproducible results across time.

## Related

- [REST Request step](/guides/workflows/rest-request-step/) — call these endpoints from a workflow
- [Generic REST connection](/reference/connectors/rest/generic-rest/) — if you'd rather configure geocoding as a saved connection
